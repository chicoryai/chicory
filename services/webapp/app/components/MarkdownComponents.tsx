/**
 * @deprecated This file is being migrated to modular components in /markdown/
 * Use the new modular components from ~/components/markdown/ instead
 * 
 * This file now simply re-exports the new modular components for backwards compatibility
 */

// Re-export everything from the new modular structure
export { markdownComponents } from './markdown';

// Legacy default export for backwards compatibility
export { markdownComponents as default } from './markdown';
