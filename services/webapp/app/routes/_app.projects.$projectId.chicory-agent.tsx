import { Outlet } from "@remix-run/react";

/**
 * Chicory Agent Layout
 * Pass-through layout - height constraints handled by FocusLayout
 */
export default function ChicoryAgentLayout() {
  return <Outlet />;
}
