# Chicory Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Required Configuration
# =============================================================================

# Anthropic API Key (required for Claude)
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=

# =============================================================================
# Storage Configuration (S3-Compatible)
# =============================================================================

# By default, Chicory uses MinIO (local S3-compatible storage)
# This works out-of-box with no additional configuration needed.

# MinIO Settings (local storage - works by default)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# S3 Bucket Names
S3_BUCKET_NAME=chicory-data
S3_BUCKET=chicory-data
TASK_AUDIT_TRAIL_S3_BUCKET_NAME=chicory-audit-trails

# S3 Endpoint (default: MinIO running in Docker)
# Change to use real AWS S3 or other S3-compatible storage
S3_ENDPOINT_URL=http://minio:9000

# AWS Credentials (defaults work with MinIO)
# For real AWS S3, replace with your AWS credentials
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_REGION=us-east-1

# =============================================================================
# Production AWS S3 Configuration (uncomment to use real AWS)
# =============================================================================

# To use real AWS S3 instead of MinIO:
# 1. Uncomment the lines below
# 2. Remove S3_ENDPOINT_URL or set it empty
# 3. Fill in your AWS credentials

# S3_ENDPOINT_URL=
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-west-2
# S3_BUCKET_NAME=your-bucket-name
# TASK_AUDIT_TRAIL_S3_BUCKET_NAME=your-audit-bucket-name

# =============================================================================
# Optional: Authentication
# =============================================================================

# By default, Chicory uses simple local auth (email/password)
# For production, you can configure PropelAuth:
# REMIX_PUBLIC_AUTH_URL=
# PROPELAUTH_API_KEY=
# PROPELAUTH_VERIFIER_KEY=

# =============================================================================
# Service Configuration (usually don't need to change)
# =============================================================================

# MongoDB
MONGO_USER=admin
MONGO_PASSWORD=chicory

# Redis
REDIS_PASSWORD=chicory

# RabbitMQ
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=chicory

# =============================================================================
# Development Settings
# =============================================================================

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Enable debug mode (set to true for development)
DEBUG=false
